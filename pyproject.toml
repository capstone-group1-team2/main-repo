[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aispire-capstone"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools]
py-modules = ["logging_config"]
packages = ["agent", "app", "ingestion", "retrieval", "eval"]